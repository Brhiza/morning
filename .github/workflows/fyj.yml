name: send fyj
on:
  workflow_dispatch:
jobs:
  send_message:
    runs-on: ubuntu-latest
    name: send card

    steps:
    - name: checkout
      uses: actions/checkout@v3
      with:
        ref: master

    - name: sender
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'
        architecture: 'x64'
    - run: pip install -r ./requirements.txt && python ./main.py

    env:
      APP_ID: ${{ secrets.APP_ID }}
      APP_SECRET: ${{ secrets.APP_SECRET }}
      TEMPLATE_ID: '_a84Q12GpifzEk8u17Rqcyobs9_evolHOItaaf3sDnM'
      USER_ID: 'oDdtc5hmGX2orBcrvO9tntdnGaMQ'
      START_DATE: '1921-07-23'
      CITY: '顺德'
